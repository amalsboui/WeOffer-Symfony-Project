{% extends 'base.html.twig' %}

{% block title %}Hello JobOffersController!{% endblock %}

{% block body %}
    {%for job in jobs %}
    <div class="card w3-theme-l5">

        <div class="card-body d-flex flex-column">
            <div class="card-title" > {{  job.postion}}</div>
            <span class="text-muted font-italic "><?php calculate_time($job["created_at"])?></span>
            <div class="tags mb-2 mt-1">
                <span class="badge bg-secondary " >{{  job.employment_type}}</span>
                <span class="badge bg-primary">{{ job["category"]}} </span>
            </div>
            <p class="card-text">{{ job["description"]}}</p>
            <ul class="list-group list-group-flush ">
                <li class="list-group-item w3-theme-l5">🧱{{ job["entreprise"] }}</li>
                <li class="list-group-item w3-theme-l5">📍 j{{ ob["location"] }}</li>
            </ul>
            <!-- chanit href-->
            <a href={{ path('job')}} class="btn btn-primary w3-theme-d4 align-self-end mt-auto">See More</a>

        </div>

    </div>
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/yasmine to main process/Symfony-Project/src/Controller/JobOffersController.php</code></li>
        <li>Your template at <code>C:/yasmine to main process/Symfony-Project/templates/job_offers/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
